services:
  frontend:
    build: .
    container_name: cdss-frontend
    restart: unless-stopped
    ports:
      - "3006:3000"
    environment:
      - NEXT_PUBLIC_API_URL=https://backend.eliuth.dev
